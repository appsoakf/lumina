将本次对话中对代码和架构的修改同步到 CLAUDE.md。

## 执行步骤

1. 回顾当前对话中所有代码修改（新增、删除、重构的文件和逻辑）
2. 读取现有 `CLAUDE.md` 全文
3. 筛选出属于"核心架构/实现变更"的修改，忽略：
   - 细微 bug 修复（如拼写、off-by-one）
   - 纯格式调整（空行、缩进）
   - 资源文件变更（音频、图片、模型）
4. 用 Edit 工具增量更新 CLAUDE.md 对应章节：
   - 新增模块 → 更新目录结构和关键文件速查表
   - 数据流变更 → 更新核心数据流章节
   - 新增配置项 → 更新外部依赖配置章节
   - 协议变更 → 更新 WebSocket 协议章节
   - 并发模型变更 → 更新并发模型章节
5. 保持 CLAUDE.md 原有 Markdown 风格和结构
6. 控制总行数不超过 150 行，必要时精简旧内容

## 约束

- 只做增量更新，不重写整个文件
- 如果本次对话没有核心变更，直接告知用户无需同步
- 更新完成后展示修改的章节名称和摘要
